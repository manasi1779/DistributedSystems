class X {

    public static void main( String args[] ) {
    
	int n = 0;
	
	here:

	while ( true )  {
		// loop is executed for start value of n = 0, n = 4
		while ( ( ( n != 3 ) || ( n != 5 ) ) && ( n < 4 ) )  {
			//value of n is 1 while executing below statement
			//hence equality is false and statement a is not printed
			if ( ++n == 0 )
				System.out.println("a/	n is " + n );
			//value of n is 2 after below statement is executed
			//hence equality is true for n = 1 and statement b is not printed
			else if ( n++ == 1 )    
				System.out.println("b/	n is " + n );
			//value of n is 2 while executing this statement and 3 after it is executed
			//Hence equality is true and statement c is printed
			else if ( n++ == 2 )
				System.out.println("c/	n is " + n );
			//else block is not executed since above if statement was true.
			else 
				System.out.println("d/	n is " + n );

			System.out.println("	executing break here");
		}
		
		System.out.println( n % 2 == 0 ?
					( n == 4 ? "=" : "!" )
				      : ( n % 3 != 0 ? "3" : "!3" ));
		break here;
	}
    }
}